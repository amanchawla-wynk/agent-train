services:
  postgres:
    image: postgres:16-alpine
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: agenttrain
      POSTGRES_PASSWORD: agenttrain
      POSTGRES_DB: agenttrain
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
